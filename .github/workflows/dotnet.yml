name: .NET 10

on:
  push:
    paths-ignore:
      - '*.md'

  pull_request:
    paths-ignore:
      - '*.md'

jobs:
  build:
    outputs:
      product_version: ${{ steps.get_release_name.outputs.PRODUCT }}

    runs-on: windows-2025-vs2026

    env:
      DOTNET_CLI_TELEMETRY_OPTOUT : true

    steps:

    - uses: actions/checkout@v4
      with:
        fetch-depth: 0 # avoid shallow clone so nbgv can do its work.
        
    - name: setup-msbuild
      uses: microsoft/setup-msbuild@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.x'

    - name: Install dependencies
      run: dotnet restore -r win-x64

    - name: Enable developer command prompt
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x86

    - name: Build
      run: dotnet build .\OsoyoosLauncher\OsoyoosLauncher.csproj --configuration Release -p:Platform=x64 --no-restore

    - name: Test
      run: dotnet test .\OsoyoosLauncher\OsoyoosLauncher.csproj --no-restore --verbosity normal -p:Platform=x64

    - name: Publish
      run: dotnet publish .\OsoyoosLauncher\OsoyoosLauncher.csproj -c Release -r win-x64 -p:Platform=x64 -P:PublishSingleFile=true -p:PublishTrimmed=false --self-contained false

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: OsoyoosLauncher
        path: OsoyoosLauncher\bin\x64\Release\win-x64\publish\

    - name: Get release name
      shell: pwsh
      id: get_release_name
      run: echo "PRODUCT=$((Get-Item -Path 'OsoyoosLauncher\bin\x64\Release\win-x64\publish\Osoyoos.exe').VersionInfo.ProductVersion)" >> $env:GITHUB_OUTPUT
    
        
  release:
    if: |
      github.event.action != 'pull_request' &&
      (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main') &&
      github.repository == 'MtnDewIt/Osoyoos-Launcher'

    needs: build
    runs-on: ubuntu-latest

    steps:
    
    - uses: actions/download-artifact@v4
      with:
        name: OsoyoosLauncher
        path: OsoyoosLauncher_win64
     
    - uses: ncipollo/release-action@v1
      with:
        prerelease: true
        token: ${{ secrets.GITHUB_TOKEN }}
        artifacts: OsoyoosLauncher_win64/*
        tag: ${{ needs.build.outputs.product_version }}
        
